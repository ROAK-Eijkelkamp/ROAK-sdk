[metadata]
name = roak-sdk
version = 0.1.0
description = Python SDK for ROAK platform
author = Emil
author_email = your.email@example.com

[options]
package_dir =
    = src
packages = find:

[options.packages.find]
where = src
