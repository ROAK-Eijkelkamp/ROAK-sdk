from .project import Project

class Customer(Project):
    """
    Represents a Customer within the ROAK system.

    Basically provides all the same functionality as Project, except the possibility to get the sites.
    """
    
    pass